# node-gtk

```bash
# Install required packages
sudo apt-get install \
  build-essential git \
  nodejs \
  gobject-introspection \
  libgirepository1.0-dev

# Install node-gtk
npm install

# Run the app
node maingtk.js
```